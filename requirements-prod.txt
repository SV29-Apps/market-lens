# Runtime dependencies for hosting (slim — the engine fetches Yahoo over urllib,
# so no yfinance; charts are drawn in the browser, so no matplotlib).
numpy>=1.26
pandas>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
tradingview-screener>=3.1.0   # whole-market "Strong right now" scan
